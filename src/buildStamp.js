/**
 * Injected at compile time by Vite (`vite.config.js` `define`).
 * If the UI never shows a new time after you pull code, the browser is still
 * using an old bundle — hard-refresh (Cmd+Shift+R) or redeploy `dist/`.
 */
export const BUILD_STAMP = __BUILD_STAMP__
