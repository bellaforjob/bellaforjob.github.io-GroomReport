/**
 * Default grooming report form artwork (background layer under all overlays).
 * Replace `src/assets/grooming-template.png` or point this import at another file.
 */
import groomingTemplatePng from '../assets/grooming-template.png'

export const DEFAULT_GROOMING_TEMPLATE = groomingTemplatePng
